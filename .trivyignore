# Intentionally empty.
#
# This image is built exactly the way upstream prescribes: their git tag, their
# uv.lock, no overrides and no pinning of our own. The CVE posture is therefore
# whatever upstream ships, reported honestly rather than suppressed.
#
# Entries get added here only after a deliberate accept-the-risk decision, each
# with the reasoning written down.
